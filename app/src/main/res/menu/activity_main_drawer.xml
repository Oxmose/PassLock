<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="none" android:id="@+id/menu_top">
        <item
            android:id="@+id/nav_search"
            android:icon="@drawable/ic_search"
            android:title="Search" />
    </group>

    <group android:checkableBehavior="none" android:id="@+id/menu_middle">
        <item
            android:id="@+id/nav_passwords"
            android:icon="@drawable/ic_lock_outline"
            android:title="Passwords" />
        <item
            android:id="@+id/nav_pins"
            android:icon="@drawable/ic_credit_card"
            android:title="Pins" />
        <item
            android:id="@+id/nav_digicodes"
            android:icon="@drawable/ic_dialpad"
            android:title="Digicodes" />
    </group>

    <group android:checkableBehavior="none" android:id="@+id/menu_bottom">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Settings" />
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_exit_to_app"
            android:title="Logout" />
    </group>

</menu>
